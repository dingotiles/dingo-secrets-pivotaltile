form_types:
- name: broker
  label: Service Broker
  description: After initial deployment, provide VAULT_TOKEN
  property_inputs:
    - reference: .properties.vault_token
      label: Vault token
      description: Vault token from vault initialization
