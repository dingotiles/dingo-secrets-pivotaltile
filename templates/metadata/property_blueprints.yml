---
property_blueprints:
- name: broker_username
  type: secret
- name: broker_password
  type: secret

- name: vault_token
  type: secret
  configurable: true
  default: 'Update this field after deployment and running "vault init"'
